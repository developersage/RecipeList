package com.example.recipelist.view

import androidx.appcompat.app.AppCompatActivity
import com.example.recipelist.R

/**
 * Make a cooking recipe list:

your app should display a list of Recipes in a recycler view.
Each recipe should have a name, description and list of ingredients(can be strings).
Your app should have the ability to add a new recipe to the database
Should be able to delete a recipe from the database or change an existing recipe.
 * */

class MainActivity : AppCompatActivity(R.layout.activity_main)